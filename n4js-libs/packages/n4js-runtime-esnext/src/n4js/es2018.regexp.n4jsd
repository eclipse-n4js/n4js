@@Global @@ProvidedByRuntime

export external public interface ~RegExpMatchArray {
	groups: ~Object with {  };
}

export external public interface ~RegExpExecArray {
	groups: ~Object with {  };
}

@Polyfill
export external public class RegExp extends RegExp {
	public get dotAll(): boolean;
}
